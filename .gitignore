# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
*.egg

# ── 가상환경 (uv) ────────────────────────────────────────
.venv/
.venv-pycaret/

# ── Jupyter ─────────────────────────────────────────────
.ipynb_checkpoints/

# ── CatBoost 학습 로그(실행할 때마다 자동 생성) ─────────────
catboost_info/

# ── 데이터 / 제출 파일 (재현 가능한 산출물, 용량·배포 이슈로 제외) ──
data/*.csv
submission/*.csv
logs.log

# ── 작업 스크래치 (최종본은 리포 루트에 별도로 커밋) ─────────
Claude outputs/

# ── OS ──────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
